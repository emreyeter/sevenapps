import { View } from 'react-native';

import { styled } from '../styled';

export const CardRoot = styled(View, {
  base: 'rounded-xl border border-neutral-200 bg-white dark:border-neutral-800 dark:bg-neutral-900',
});
